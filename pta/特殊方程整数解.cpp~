#include<iostream>

using namespace std;

int main(){
  int num;
  cin>>num;

  for(int i = 0;i < num/2 ; i++){
    for(int j = 0;j < num/2;j++)
      if(i*i + j*j == num)
	cout<<i<<" "<<j<<endl;
  }
  
  return 0;
}
