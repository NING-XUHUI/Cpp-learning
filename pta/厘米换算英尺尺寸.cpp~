#include

using namespace std;

int main(){

  int cm;
  cin>>cm;
  int inch = 120*cm/(145*0.3048);
  int inch_bit = inch % 12;
  int count = 0;
  int foot_cnt = inch;
  while(foot_cnt > inch_bit){
    foot_cnt - 12;
    count++;
  }
  cout<<cout<<" "<<inch_bit;

  

  return 0;
}
