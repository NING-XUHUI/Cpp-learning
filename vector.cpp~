#include<iostream>
#include<vector>

using namespace std;

itn main(){
  unsigned n;
  cout<<"n = "<<endl;
  cin>>n;

  vector<double>arr(n);
  cout<<"Please input "<<n<<"real numbers:"<<endl;

  for(unsigned i = 0;i<n;i++){
    sum += arr[i];
  }
  return sum/arr.size();
  
  return 0;
}
