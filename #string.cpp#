#include<iostream>
#include<string>

using namespace std;

int main(){

  

  return 0;
}


/*
string();默认构造函数，建立一个长度为零的串
例如：string s1;

string(const char *s);用指针s所指向的字符串是常量初始化string对象
例如：
sting s2 = “abc”；

string（const string & rhs）；复制构造函数
例如：
string s3 = s2；

*/

/*string类常用操作
s+t  将s和t连接成一个新串
s=t  用t更新s
s==t 判断s和t是否相等
s！=t判断s和t是否不等
s<t
...
*/